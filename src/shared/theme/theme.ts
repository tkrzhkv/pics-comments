import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: `'Rounded Mplus 1c', sans-serif`,
    body: `'Rounded Mplus 1c', sans-serif`,
  },
});

export default theme;
